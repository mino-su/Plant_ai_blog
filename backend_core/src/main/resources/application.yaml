# [기본 및 로컬 개발 환경 설정]
spring:
  profiles:
    active: local
  
  datasource:

    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/plant}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:1234}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update # 로컬에서는 개발 편의를 위해 update 유지
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  sql:
    init:
      mode: always

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

  data:
    redis:

      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: 6379

  flask:
    api:
      url: ${FLASK_AI_HOST:http://localhost:5000}

jwt:

  secret: ${SPRING_JWT_SECRET:ThisIsADefaultSecretKeyForLocalDevelopmentAndTestingPurposes12345}

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.sql: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

file:
  upload-dir: ./uploads/

cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}